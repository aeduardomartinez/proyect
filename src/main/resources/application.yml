spring:
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update  # Esta línea es opcional, si quieres que Hibernate actualice automáticamente el esquema de la base de datos
  datasource:
    url: jdbc:postgresql://bdprueba.c7y8yygosj15.us-east-2.rds.amazonaws.com:5432/postgres
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
    platform: postgres
server:
  port: 8080
